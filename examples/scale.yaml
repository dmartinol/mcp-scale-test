# Example configuration for testing an SSE everything MCP server using the echo tool
server:
  transport: sse
  host: localhost
  port: 8080
  path: /sse

test:
  tool_name: echo
  tool_args:
    message: "Hello from load test"
  concurrent_requests: 20
  duration_seconds: 60